[project]
name = "steam-analyzer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "dash>=4.1.0",
    "pandas>=3.0.3",
]

[dependency-groups]
dev = [
    "ipykernel>=7.2.0",
]
